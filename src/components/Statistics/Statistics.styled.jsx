import styled from "styled-components";

export const StatisticsList = styled.ul`
    display: flex;
    flex-direction: column;
padding: 10px;
    gap: 15px;

    font-size:24px;
    font-weight: 500;
`